.DS_Store
.gitignore
node_modules
public
prof
tsconfig.tsbuildinfo
.obsidian
.quartz-cache
private/
.replit
replit.nix

# content/ is tracked by its own independent git repo.
# Keep the upstream .gitkeep marker; ignore everything else.
/content/*
!/content/.gitkeep
